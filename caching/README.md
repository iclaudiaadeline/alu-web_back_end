# Caching project

This folder contains implementations of several cache replacement policies used
for learning purposes: Basic (no limit), FIFO, LIFO, LRU and MRU.

Each cache class inherits from `base_caching.BaseCaching` and follows the
project requirements (shebang, docstrings). See each module for usage.
